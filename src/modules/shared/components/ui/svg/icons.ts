/**
 * CAUTION:
 * Use this sparingly, these SVGs are not optimized for performance.
 * They are meant to be used as React components, which means they will be bundled with the application,
 * increasing the bundle size. Users have to download more js.
 * If it is not necessary to use them as React components, use as URLs instead.
 */
export { default as react } from '@/assets/svgr/react.svg?react'
